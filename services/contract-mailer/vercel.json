{
  "$schema": "https://openapi.vercel.sh/vercel.json",

  "//": "The glob rather than a named file, so a route added here cannot silently miss the pinned runtime.",

  "functions": {
    "api/*.ts": {
      "runtime": "@vercel/node@5",
      "maxDuration": 15
    }
  }
}
