"use client";

import { collection, getDocs, limit, orderBy, query } from "firebase/firestore";
import { BarChart3, CircleDollarSign, CreditCard, HandCoins, ReceiptText, RefreshCw, WalletCards } from "lucide-react";
import { useCallback, useEffect, useState } from "react";
import { AppShell } from "./app-shell";
import { useFirebaseAuth } from "./firebase-provider";
import { getFirebaseClient } from "@/lib/firebase/client";
import { firebaseErrorMessage, formatDate, formatMoney } from "@/lib/presentation";
import { callRentalFunction, type FinancialOverview as FinancialData } from "@/lib/services/functions-client";

type VehicleOption = { id: string; label: string };
function toDateInput(date: Date): string { return date.toISOString().slice(0, 10); }
function startOfMonth(): string { const date = new Date(); return toDateInput(new Date(date.getFullYear(), date.getMonth(), 1)); }

export function FinanceOverview() {
  const { role } = useFirebaseAuth();
  const [from, setFrom] = useState(startOfMonth); const [to, setTo] = useState(() => toDateInput(new Date()));
  const [data, setData] = useState<FinancialData>(); const [vehicles, setVehicles] = useState<VehicleOption[]>([]); const [error, setError] = useState<string>(); const [notice, setNotice] = useState<string>(); const [loading, setLoading] = useState(true); const [savingExpense, setSavingExpense] = useState(false);
  const load = useCallback(async () => { setLoading(true); setError(undefined); try { const [overview, fleet] = await Promise.all([callRentalFunction<{ from: string; to: string }, FinancialData>("getFinancialOverview", { from, to }), getDocs(query(collection(getFirebaseClient().db, "vehicles"), orderBy("registrationNumber"), limit(100)))]); setData(overview); setVehicles(fleet.docs.map((document) => ({ id: document.id, label: `${document.get("registrationNumber")} · ${document.get("make")} ${document.get("model")}` }))); } catch (cause) { setError(firebaseErrorMessage(cause)); } finally { setLoading(false); } }, [from, to]);
  useEffect(() => { if (role === "admin") void load(); }, [load, role]);
  async function addExpense(event: React.FormEvent<HTMLFormElement>) { event.preventDefault(); setSavingExpense(true); setError(undefined); setNotice(undefined); const form = new FormData(event.currentTarget); try { await callRentalFunction("recordVehicleExpense", { vehicleId: String(form.get("vehicleId")), category: String(form.get("category")), amountCents: Math.round(Number(form.get("amount")) * 100), occurredAt: new Date(`${String(form.get("occurredAt"))}T12:00:00`).toISOString(), vendor: String(form.get("vendor")).trim() || null, note: String(form.get("note")).trim(), idempotencyKey: crypto.randomUUID() }); setNotice("Expense recorded."); event.currentTarget.reset(); await load(); } catch (cause) { setError(firebaseErrorMessage(cause)); } finally { setSavingExpense(false); } }
  if (role !== "admin") return <AppShell title="Finance" eyebrow="Restricted"><section className="empty-state prominent"><div className="empty-illustration"><WalletCards /></div><div><h2>Administrator access required</h2><p>Financial reports and profit data are visible only to administrators.</p></div></section></AppShell>;
  const metrics = [{ label: "Invoiced", value: data?.invoicedCents, icon: ReceiptText, tone: "navy" }, { label: "Received", value: data?.receivedCents, icon: CreditCard, tone: "green" }, { label: "Expenses", value: data?.expensesCents, icon: HandCoins, tone: "orange" }, { label: "Operating margin", value: data?.operatingMarginCents, icon: BarChart3, tone: "purple" }, { label: "Outstanding", value: data?.outstandingCents, icon: CircleDollarSign, tone: "red" }];
  return <AppShell title="Finance" eyebrow="Rental performance" action={<button className="button button-secondary compact" onClick={() => void load()} disabled={loading}><RefreshCw size={16} className={loading ? "spin" : ""} /> Refresh</button>}><section className="report-filter surface"><div className="field"><label htmlFor="from">From</label><input id="from" type="date" value={from} onChange={(event) => setFrom(event.target.value)} /></div><div className="field"><label htmlFor="to">To</label><input id="to" type="date" value={to} onChange={(event) => setTo(event.target.value)} /></div><button className="button button-primary" onClick={() => void load()} disabled={loading}>Run report</button></section>{error && <div className="alert alert-error">{error}</div>}{notice && <div className="alert alert-success">{notice}</div>}<section className="metric-grid finance-metrics">{metrics.map(({ label, value, icon: Icon, tone }) => <article className="metric-card" key={label}><div className={`metric-icon ${tone}`}><Icon size={19} /></div><div><p>{label}</p><strong>{value === undefined ? "—" : formatMoney(value)}</strong></div></article>)}</section><section className="dashboard-grid finance-grid"><article className="surface"><div className="section-heading"><div><p className="section-kicker">By vehicle</p><h2>Operating margin</h2></div></div>{data?.vehiclePerformance.length ? <div className="list-table">{data.vehiclePerformance.map((vehicle) => <div className="finance-row" key={vehicle.vehicleId}><div><strong>{vehicle.vehicleRegistration}</strong><span>{formatMoney(vehicle.invoicedCents)} invoiced · {formatMoney(vehicle.expensesCents)} expenses</span></div><strong className={vehicle.operatingMarginCents < 0 ? "amount-negative" : "amount-positive"}>{formatMoney(vehicle.operatingMarginCents)}</strong></div>)}</div> : <div className="inline-empty">No financial activity in this period.</div>}</article><article className="surface"><div className="section-heading"><div><p className="section-kicker">New entry</p><h2>Record an expense</h2></div></div><form className="compact-form" onSubmit={(event) => void addExpense(event)}><div className="field"><label>Vehicle</label><select name="vehicleId" defaultValue="" required><option value="" disabled>Select a vehicle</option>{vehicles.map((vehicle) => <option value={vehicle.id} key={vehicle.id}>{vehicle.label}</option>)}</select></div><div className="form-columns"><div className="field"><label>Category</label><select name="category" defaultValue="maintenance"><option value="repair">Repair</option><option value="service">Service</option><option value="maintenance">Maintenance</option><option value="parts">Parts</option><option value="other">Other</option></select></div><div className="field"><label>Amount (USD)</label><input name="amount" type="number" min="0.01" max="100000" step="0.01" inputMode="decimal" required /></div></div><div className="form-columns"><div className="field"><label>Date</label><input name="occurredAt" type="date" defaultValue={toDateInput(new Date())} required /></div><div className="field"><label>Vendor</label><input name="vendor" maxLength={160} /></div></div><div className="field"><label>Note</label><input name="note" minLength={1} maxLength={1000} required /></div><button className="button button-primary" disabled={savingExpense}>{savingExpense ? "Saving…" : "Record expense"}</button></form></article></section><section className="surface ledger-surface"><div className="section-heading"><div><p className="section-kicker">Activity</p><h2>Recent entries</h2></div><span className="quiet">{data?.outstandingRentals ?? 0} rental{data?.outstandingRentals === 1 ? "" : "s"} outstanding</span></div>{data?.recentEntries.length ? <div className="table-wrap"><table><thead><tr><th>Date</th><th>Type</th><th>Vehicle</th><th className="align-right">Amount</th></tr></thead><tbody>{data.recentEntries.map((entry) => <tr key={entry.id}><td>{formatDate(entry.occurredAt)}</td><td><span className="entry-type">{entry.entryType.replaceAll("_", " ")}</span></td><td>{entry.vehicleRegistration}</td><td className={`align-right ${entry.amountCents < 0 ? "amount-negative" : "amount-positive"}`}>{formatMoney(entry.amountCents)}</td></tr>)}</tbody></table></div> : <div className="inline-empty">No entries for this period.</div>}</section></AppShell>;
}
