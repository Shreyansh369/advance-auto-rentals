{
  "name": "advance-auto-rentals-functions",
  "private": true,
  "main": "lib/functions/src/index.js",
  "engines": { "node": "22" },
  "scripts": { "build": "tsc", "serve": "pnpm build && firebase emulators:start --only functions", "deploy": "firebase deploy --only functions", "lint": "eslint src --max-warnings=0" },
  "dependencies": { "firebase-admin": "^13.5.0", "firebase-functions": "^6.5.0", "zod": "^4.1.8" },
  "devDependencies": { "@types/node": "^24.5.2", "typescript": "^5.9.2" }
}
